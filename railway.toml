# Создайте/обновите railway.toml
@'
[build]
builder = "nixpacks"

[[services]]
name = "web"
port = 3000
protocol = "tcp"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
startTimeout = 120
'@ | Out-File railway.toml -Encoding UTF8